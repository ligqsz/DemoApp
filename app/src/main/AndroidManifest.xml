<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pax.demoapp">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".DemoApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.PagerActivity" />
        <activity android:name=".ui.activity.EditTextActivity" />
        <activity
            android:name=".ui.activity.ToolBarActivity"
            android:theme="@style/AppThemeNoActionBar" />
        <activity android:name=".ui.activity.OrmLiteActivity" />
        <activity android:name=".ui.activity.OrmResultActivity" />
        <activity android:name=".ui.activity.GreenDaoActivity" />
        <!-- 占位符 -->
        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="${UMENG_CHANNEL}" />
        <meta-data
            android:name="SHOW_LOG"
            android:value="${SHOW_LOG}" />
        <meta-data
            android:name="SAVE_LOG"
            android:value="${SAVE_LOG}" />

        <activity android:name=".ui.activity.CustomViewActivity" />
        <activity android:name=".ui.activity.TestActivity" />
        <activity android:name=".ui.activity.AnimActivity" />
        <activity
            android:name=".ui.activity.PropertyActivity"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".ui.activity.RippleEffectActivity"
            android:theme="@style/AppTheme.Ripple" />
        <activity android:name=".ui.activity.ExposeActivity"></activity>
    </application>

</manifest>